const productController = require('../../controllers/product');
const { productValidator } = require('../../middleware');
const { urlConstants } = require('../../constants');

module.exports = (app) => {
    // app.post(urlConstants.PRODUCT, productValidator.signUpValidation, productController.signUp);
};